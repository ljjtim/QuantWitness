# 安全与敏感信息报告

QuantWitness 是供本机可信合作者使用的研究框架，不是执行不受信任 Python 的安全沙箱。项目 extension 在独立进程中运行并受资源、超时和工件边界约束，但仍应视为可信本地代码。

如果发现以下问题，请使用 GitHub 仓库的私下安全报告功能，不要先创建公开 issue：

- 仓库或发布产物包含 token、cookie、私钥、真实数据库路径或其他敏感信息；
- fork PR 可以读取发布凭据或获得不必要的上游写权限；
- 路径边界允许正式工件越过显式 staging 根；
- 只读研究流程能够修改数据源；
- 发布产物包含未允许的文件或本机路径。

普通功能缺陷、研究口径问题和文档错误可以使用公开 issue。报告中请提供受影响版本、最小复现、实际影响和建议验证方式，避免附带真实凭据或私有数据。
